# Stock4U - סכמה מפושטת

## סטטוס: ✅ הושלם

הסכמה החדשה עובדת:
- טבלת `gifts` היא הטבלה הראשית עם עמודת `token` למימוש
- פונקציית `send-smtp-email` מעדכנת token ב-gifts במקום ליצור רשומה ב-gift_registrations
- פונקציית `get-gift-details` תומכת בחיפוש לפי giftId או token
- ממשק האדמין קורא מ-gifts
